app-id: 'sh.tat.usps-track'
command: 'usps-track'
finish-args:
  - '--share=ipc'
  - '--share=network'
  - '--socket=fallback-x11'
  - '--socket=wayland'
modules:
  - build-commands:
      - 'pip3 install --prefix=/app uv'
      - '/app/bin/uv pip install --prefix=/app .'
    build-options:
      build-args:
        - '--share=network'
    buildsystem: 'simple'
    name: 'usps-track'
    sources:
      - path: '.'
        type: 'dir'
runtime: 'org.freedesktop.Platform'
runtime-version: '24.08'
sdk: 'org.freedesktop.Sdk'
